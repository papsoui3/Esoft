<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Define the border color -->
    <stroke
        android:width="0.5dp"
        android:color="#000000" /> <!-- Replace #FF0000 with the color you want for the border -->

    <!-- Define the background color of the row (optional) -->
    <solid android:color="#DCDCDC" /> <!-- Replace #FFFFFF with the background color of the row -->

    <!-- Define the padding inside the border (optional) -->
    <padding
        android:left="1dp"
        android:right="1dp"
        android:top="1dp"
        android:bottom="1dp" />


</shape>
